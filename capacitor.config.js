/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
    appId: 'com.pulsequest.app',
    appName: 'PulseQuest',
    webDir: 'web',
    server: {
        androidScheme: 'https'
    }
};

module.exports = config;
